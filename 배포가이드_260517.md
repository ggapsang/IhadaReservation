# 배포 가이드 (Phase 0 + 260522 약관 반영)

> 단일 공간 운영(이오 아크로 _성수)을 위한 Apps Script + 스프레드시트 초기 세팅 절차입니다.
>
> 반영되는 문서:
> - `요구사항명세서_260517_1.md` — Phase 0 코드 구조·결제 모듈 자리
> - `요구사항명세서_260522_1.md` — 약관 + 가격 확정안 (단일 공간, 3시간 패키지, 평일/주말 차등, 보증금, 환불 정책)

---

## 1. 파일별 함수 매핑

Apps Script 에디터에서 함수 드롭다운에 표시되는 함수의 소속 파일입니다. 운영자가 호출할 일이 있는 함수만 정리합니다.

### `01_Setup.gs` — 초기 세팅 통합 (운영자가 가장 자주 보는 파일)
| 함수 | 용도 |
|---|---|
| **`setupAll`** | **초기 배포 시 1회 실행**. 경고창 후 시트 데이터 삭제 + 전체 초기화. |
| `setupAll_keepData` | 운영 중 마이그레이션용. 데이터 유지하며 누락 항목만 보강. |

### `10_Config.gs` — 설정 & Properties
| 함수 | 용도 |
|---|---|
| `getSettings` | 설정 시트 전체 조회 (클라이언트 호출). |
| `getRoomInfo` | (Deprecated) 단일 공간 운영이라 빈 배열 반환. |
| `getConfig` | Script Property 단일 키 조회. |
| `initializeProperties` | Property 12종 생성 (`setupAll` 내부 호출, 단독 실행도 가능). |
| `migrateSettings_260522` | 약관 가격·정책 설정 시트에 적용 (`setupAll` 내부 호출). |

### `20_Util.gs` — 헬퍼·표준 응답·로깅
운영자가 직접 실행할 함수는 없음. 다른 모든 파일이 의존하는 라이브러리 역할.

### `30_Reservation.gs` — 예약 등록
| 함수 | 용도 |
|---|---|
| `submitReservation` | 폼 제출 진입점 (클라이언트 호출). |
| `generateReservationNumber` | 예약번호 발급 (내부). |
| `validateFormData` | 폼 유효성 (내부). |

### `40_Pricing.gs` — 가격 계산
| 함수 | 용도 |
|---|---|
| `calculatePrice` | 실시간 가격 미리보기 (클라이언트 호출). |
| `calculateTotalPrice` | 옵션 포함 통합 가격 계산. |
| `calculateRefundAmount` | 환불 금액 계산 (260522 일 단위 정책). |
| `isWeekendOrHoliday` | 평일/주말·공휴일 판정. |
| `getOptionProducts` | 옵션 상품 시트 조회 (Phase 1 자리). |

### `50_Availability.gs` — 가용성
| 함수 | 용도 |
|---|---|
| `checkAvailability` | 시간대 충돌 확인 (클라이언트 호출). |
| `getReservedSlotsByDate` | 지정일 점유 시간대 목록. |

### `60_Payment.gs` — 결제 모듈 (stub) + 스키마 부트스트랩
| 함수 | 용도 |
|---|---|
| `initializePaymentSchema` | 신규 32컬럼 헤더 + 결제 보조 시트 생성 (`setupAll` 내부 호출). |
| `createPaymentOrder` / `verifyPayment` / `cancelPayment` / `refundPayment` / `processPaymentWebhook` | Phase 1 자리, 모두 `NOT_IMPLEMENTED` 응답. |

### `70_Integration.gs` — Calendar / Drive / Docs
| 함수 | 용도 |
|---|---|
| `getTermsContent` | 약관 Docs → HTML (클라이언트 호출). |
| `createCalendarEvent` | Calendar 이벤트 생성. |
| `syncToCalendar` | 입금확인(S열) → Calendar 일괄 동기화 (운영자가 시트 메뉴/버튼에서 수동 실행). |
| `uploadFile` | 사업자등록증 Drive 업로드. |
| `testDrivePermission` / `testCalendarPermission` | 권한 점검용. |

### `90_Test.gs` — 검증
| 함수 | 용도 |
|---|---|
| `_verifyPhase0` | 인수 기준 자동 검증. 세팅 후 1회 실행. |
| `_verifyRefundPolicy_260522` | 환불 정책 단독 검증. |
| `_testMasking` | 로그 마스킹 검증. |

### `00_Entry.gs` — WebApp 진입점
운영자가 직접 실행할 함수 없음. `doGet`/`doPost`만 정의.

---

## 2. 사전 준비 — 백업 (5분)

작업 전 반드시 백업합니다. 중간 단계에서 문제가 생겨도 되돌릴 수 있어야 합니다.

### 2-1. 스프레드시트 백업
1. 운영 중인 예약 관리 스프레드시트를 엽니다.
2. 메뉴: `파일 → 사본 만들기`
3. 사본 이름: `예약관리시스템_백업_260522_초기배포이전`
4. 본인 드라이브에 저장.

### 2-2. 기존 Apps Script 코드 백업 (있는 경우)
1. `확장 프로그램 → Apps Script` 진입.
2. 좌측 파일 목록의 각 `.gs` 파일 내용을 로컬 텍스트 파일로 복사 저장.

---

## 3. Apps Script 파일 반영 (15분)

본 저장소(`02_IhadaReservation` 폴더)의 10개 `.gs` 파일을 Apps Script 에디터에 동일 이름으로 생성하고 내용을 붙여넣습니다.

| 순서 | 파일명 |
|---|---|
| 1 | `00_Entry` |
| 2 | `01_Setup` |
| 3 | `10_Config` |
| 4 | `20_Util` |
| 5 | `30_Reservation` |
| 6 | `40_Pricing` |
| 7 | `50_Availability` |
| 8 | `60_Payment` |
| 9 | `70_Integration` |
| 10 | `90_Test` |

각 파일 붙여넣은 직후 `Ctrl+S` 저장. 그리고 `index.html`도 동일하게 생성하여 붙여넣습니다.

> 기존 운영 환경에 다른 `.gs` 파일이 남아 있다면 함수명 충돌이 발생할 수 있으니 새 10개 외의 파일은 삭제하십시오.

---

## 4. 초기 세팅 — `setupAll()` 1회 실행 (2분)

⚠️ **이 단계에서 기존 시트 데이터가 삭제됩니다.** 백업이 끝났는지 확인 후 진행하십시오.

1. Apps Script 에디터 상단 함수 드롭다운에서 **`setupAll`** 선택.
2. ▶ `실행`.
3. 권한 승인 팝업이 처음 뜨면 본인 계정으로 승인.
4. 경고창이 표시됩니다:
   ```
   ⚠️ 초기 세팅
   1) 예약내역·예약현황로그의 모든 데이터를 삭제합니다.
   2) 레거시 시트(Room정보·옵션상품·임시주문·결제로그)를 통째로 삭제합니다.
   3) 설정 시트의 옛 키를 모두 제거하고 명세 기준 키만 남깁니다.
   4) Properties를 빈 값으로 초기화합니다.
   5) 예약내역 25컬럼 헤더를 새로 작성합니다.
   6) 명세 기준 가격·정책을 설정 시트에 작성합니다.
   계속하시겠습니까?
   ```
   - **[예]**: 위 4단계를 모두 수행.
   - **[아니요]**: 즉시 중단 (`{aborted: true}` 반환). 데이터 보존하며 누락분만 보강하려면 `setupAll_keepData()`를 대신 실행.
5. 완료 알림창이 표시되면 OK.

### 결과 확인
스프레드시트 탭에서 직접 확인합니다.

**예약내역 시트 헤더 (25컬럼, 명세 항목만)**
```
A 예약번호 | B 신청일시 | C 예약날짜 | D 시작시간 | E 종료시간 | F 이용시간
G 이름 | H 연락처 | I 이메일 | J 전체인원 | K 세금계산서
L 기본요금 | M 시간추가요금 | N 인원추가요금 | O 소계 | P VAT | Q 보증금 | R 총금액
S 입금확인 | T 입금확인일시 | U 사업자등록증
V Calendar이벤트ID | W 알림톡발송상태
X 환불금액 | Y 환불일시
```

**레거시 시트는 모두 삭제되어 있어야 합니다** (Room정보·옵션상품·임시주문·결제로그). 결제 통합 시점(Phase 1)에 필요한 시트가 다시 생성됩니다.

**설정 시트 (260522 약관 정책)**
| 항목명 | 값 | 단위 | 설명 |
|---|---|---|---|
| 기준인원 | 4 | 명 | 기본 이용 인원 |
| 최대인원 | 8 | 명 | 입실 가능 최대 인원 |
| 최소이용시간 | 3 | 시간 | 3시간 패키지 |
| 기본요금_평일_3시간 | 300000 | 원 | 평일 월~금 (VAT 포함) |
| 기본요금_주말_3시간 | 400000 | 원 | 주말·공휴일 (VAT 포함) |
| 시간추가요금 | 50000 | 원/시간 | 3시간 초과 1시간당 |
| 인원추가요금 | 10000 | 원/인 | 기준 초과 1인당 |
| 보증금 | 100000 | 원 | 예약금과 함께 수령, 퇴실 후 환불 |
| VAT포함여부 | Y | | 가격이 VAT 포함 |
| 환불정책_8일이상 | 100 | % | 이용일 8일 전까지 |
| 환불정책_5_7일 | 50 | % | 7~5일 전 |
| 환불정책_3_4일 | 30 | % | 4~3일 전 |
| 환불정책_2일이내 | 0 | % | 2일 전~당일 |

**Properties 12개** (좌측 톱니바퀴 `프로젝트 설정` → `스크립트 속성`)
```
PAYMENT_PROVIDER (=tosspayments) / PAYMENT_CLIENT_KEY / PAYMENT_SECRET_KEY / PAYMENT_WEBHOOK_SECRET
KAKAO_API_KEY / KAKAO_SENDER_KEY
ADMIN_EMAIL / ADMIN_PHONE
CALENDAR_ID_CONFIRMED / CALENDAR_ID_PENDING
UPLOAD_FOLDER_ID
TERMS_DOC_ID
```
값은 모두 빈 상태 (7절에서 입력).

---

## 5. (선택) 공휴일 등록

공휴일에 주말 가격(400,000원)을 자동 적용하려면 설정 시트에 1행을 추가합니다.

| 항목명 | 값 | 단위 |
|---|---|---|
| 공휴일목록 | `2026-05-25,2026-06-06,2026-08-15` | (쉼표 구분 YYYY-MM-DD) |

키가 없으면 토·일만 주말로 자동 판정됩니다.

---

## 6. 자동 검증 — `_verifyPhase0()` (1분)

1. Apps Script 에디터 함수 드롭다운에서 **`_verifyPhase0`** 선택.
2. ▶ `실행`.
3. 실행 로그(`보기 → 실행`)에서:
   ```
   === _verifyPhase0 결과 ===
   통과: N / N
   ```
   모든 항목이 통과해야 합니다. 실패 항목이 있으면 항목명과 detail이 함께 출력됩니다.

### 환불 정책 단독 검증 (선택)
함수 드롭다운에서 **`_verifyRefundPolicy_260522`** 실행 → 10일 전(100%) / 6일 전(50%) / 4일 전(30%) / 1일 전(0%) 모두 `ok: true` 확인.

### 마스킹 검증 (선택)
함수 드롭다운에서 **`_testMasking`** 실행 → `결제로그` 시트 마지막 행의 I열에서 카드번호·시크릿 키가 마스킹되어 있는지 확인.

---

## 7. 운영자 정보 입력 — Properties 채우기

Apps Script 에디터 좌측 톱니바퀴 → `프로젝트 설정` → `스크립트 속성` 섹션에서 다음을 입력합니다.

### 즉시 입력 (운영 시작 전 필수)
| Property | 값 |
|---|---|
| `ADMIN_EMAIL` | `eoaceo@gmail.com` (약관 기준) |
| `ADMIN_PHONE` | `010-9983-0025` 또는 `010-4499-4734` (약관 기준) |
| `CALENDAR_ID_CONFIRMED` | eoacro 캘린더 ID. **10절 캘린더 연동 절차 참고**. 비워두면 운영자 개인 기본 캘린더에 등록됨 — 반드시 입력 권장 |
| `TERMS_DOC_ID` | 더 이상 사용되지 않습니다 (약관 본문이 코드에 박혀있음). 비워두십시오 |

### 결제·알림톡 API 키 (현재 미발급, 발급 후 입력)
| Property | 비고 |
|---|---|
| `PAYMENT_CLIENT_KEY` / `PAYMENT_SECRET_KEY` / `PAYMENT_WEBHOOK_SECRET` | 토스페이먼츠 발급 시 입력 |
| `KAKAO_API_KEY` / `KAKAO_SENDER_KEY` | 알림톡 발급 시 입력 |

`UPLOAD_FOLDER_ID`는 첫 사업자등록증 업로드 시 자동 생성됩니다.

---

## 8. 약관 본문

요구사항 명세서_260522 약관 본문은 `70_Integration.gs::TERMS_HTML_260522` 상수에 코드로 박혀 있습니다. 운영자가 별도로 등록할 필요 없습니다. 향후 약관 개정 시 그 상수만 새 본문으로 교체하고 재배포합니다.

---

## 9. WebApp 재배포 (2분)

1. 우측 상단 `배포 → 배포 관리` → 기존 배포 옆 연필 아이콘.
2. `버전` 드롭다운 → `새 버전` → 설명 입력 (예: `260522 약관 반영 초기 배포`).
3. `배포` 클릭.
4. 발급된 URL을 외부 안내. (기존 배포 업데이트 시 URL 동일)

---

## 10. 수동 회귀 테스트 (10분)

| # | 시나리오 | 기대 결과 |
|---|---|---|
| 1 | 폼 로드 | F12 콘솔에 `설정 로드 완료`. 헤더 "이오 아크로 _성수 예약". 시간 옵션 00:00~24:00 30분 단위 |
| 2 | 폼 필드 확인 | 이름·연락처·이메일·날짜·시간·인원·세금계산서·약관 동의만 표시. Room/업체명/인스타/차량/유입경로/촬영내용 없음 |
| 3 (평일) | 평일 날짜 + 10:00-13:00 + 인원 5 | 기본 요금 300,000 / 시간 추가 0 / 인원 추가 30,000 (1명×3시간×10,000) / 소계 330,000 / 보증금 100,000 / **총 430,000원** |
| 4 (주말) | 토요일 + 10:00-14:00 + 인원 4 | 기본 요금 400,000 / 시간 추가 50,000 / 인원 추가 0 / 소계 450,000 / 보증금 100,000 / **총 550,000원** |
| 4-2 (평일) | 평일 + 10:00-14:00 + 인원 6 | 기본 300,000 + 시간추가 50,000 + 인원추가 80,000(2명×4시간×10,000) = 소계 430,000 / 보증금 100,000 / **총 530,000원** |
| 5 | 10:00-12:00 입력 후 제출 | "최소 이용 시간은 3시간입니다" 에러 |
| 6 | 인원 9 입력 | 브라우저 max=8 가드. 우회 시 "최대 입실 가능 인원은 8명입니다" |
| 7 | 이메일 미입력 | "이메일을 입력해주세요" / 형식 오류 시 "이메일 형식이 올바르지 않습니다" |
| 8 | 테스트 예약 제출 | 알림창에 예약번호 + 총 결제 금액 + 보증금 안내. 예약내역 시트 G열(이름)부터 정상 기재 |
| 9 | 시트 S열(입금확인) 셀에 `Y` 입력 또는 체크 | 자동 트리거(`onEdit`)로 Calendar 이벤트 생성, V열에 eventId, T열에 시각 자동 기록 |
| 10 | 시트 메뉴 `예약 관리 > 캘린더 동기화` 실행 | 미동기화 행 일괄 처리, 처리된 예약번호 목록 표시 |
| 11 | 폼 약관 모달 | 260522 약관 본문 표시 (제1조~제8조 모두) |
| 12 | 세금계산서=Y + 사업자등록증 업로드 | Drive 업로드. U열에 URL 기록 |

---

## 11. 캘린더 연동 (필수 운영 절차)

요구사항 명세서_260522 제2조의 "eoacro 캘린더 등록"을 자동화합니다.

### 11-1. 캘린더 준비
1. Google Calendar(`calendar.google.com`)에서 운영용 캘린더를 생성하거나 기존 캘린더를 선택합니다(예: "이오 아크로 _성수 예약").
2. 좌측 캘린더 목록에서 해당 캘린더 옆 ⋮ → `설정 및 공유`.
3. 페이지 하단 `캘린더 통합` 섹션의 **캘린더 ID**를 복사합니다(예: `xxxxxxxx@group.calendar.google.com`).

### 11-2. Property 입력
1. Apps Script 에디터 → 좌측 톱니바퀴 `프로젝트 설정` → `스크립트 속성`.
2. `CALENDAR_ID_CONFIRMED` 옆 연필 → 11-1에서 복사한 캘린더 ID 입력 → 저장.
3. 비워두면 운영자 개인 기본 캘린더에 등록되어 혼선이 생기므로 반드시 입력 권장.

### 11-3. 권한 승인 (최초 1회)
Apps Script 에디터 함수 드롭다운에서 **`testCalendarPermission`** 선택 → ▶ 실행. 권한 승인 팝업이 뜨면 본인 계정으로 승인.

### 11-4. onEdit 자동 트리거 — 입금 확인 즉시 자동 등록
운영자가 시트 `예약내역`의 S열(`입금확인`)에 `Y` 입력 또는 체크박스 ON으로 변경하는 즉시:

- `70_Integration.gs::onEdit`이 자동 실행됩니다 (Apps Script simple trigger).
- 해당 행을 `createCalendarEvent`로 캘린더에 등록합니다.
- V열(`Calendar이벤트ID`)에 생성된 이벤트 ID가 기록됩니다.
- T열(`입금확인일시`)이 비어 있으면 처리 시각이 기록됩니다.
- 이미 V열에 eventId가 있으면 중복 등록 방지를 위해 건너뜁니다.

> simple trigger는 스프레드시트 소유자가 직접 편집할 때만 동작합니다. 다른 계정의 편집에도 자동 등록되어야 한다면 Apps Script 에디터 → 좌측 시계 아이콘 → `트리거 추가` → `onEdit` / 이벤트 유형 `편집 시` / 소스 `스프레드시트에서`로 installable trigger를 별도 설치하십시오.

### 11-5. 일괄 동기화 — 메뉴 `예약 관리 > 캘린더 동기화`
스프레드시트를 열면 상단에 `예약 관리` 메뉴가 자동으로 추가됩니다(`onOpen`). 클릭 → `캘린더 동기화 (일괄)` 선택 시 미동기화 행을 한 번에 처리합니다.

- 트리거가 누락된 과거 행을 일괄 처리할 때 사용합니다.
- 처리 기준: S열=Y AND V열 비어있음.

### 11-6. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| S열에 Y를 입력해도 캘린더에 등록 안 됨 | onEdit simple trigger가 다른 계정의 편집을 인식 못함 | 11-4 하단 안내대로 installable trigger 설치 |
| 운영자 개인 캘린더에 등록됨 | `CALENDAR_ID_CONFIRMED` 미설정 | 11-2 절차로 ID 입력 |
| "Calendar를 찾을 수 없습니다" 오류 | 잘못된 캘린더 ID 또는 공유 권한 없음 | 11-1에서 ID 재확인. 공유 설정에서 운영자 계정이 변경 권한 보유하는지 확인 |
| 같은 예약이 여러 번 등록됨 | V열의 eventId가 삭제되어 중복 처리됨 | V열 값을 보존. 캘린더에서 직접 중복 이벤트 삭제 후 V열 정합 |
| `예약 관리` 메뉴가 안 보임 | `onOpen`이 호출되지 않음 (Apps Script 코드 미배포 또는 권한 부족) | 시트 새로고침. 그래도 안 보이면 함수 드롭다운에서 `onOpen` 한 번 직접 실행하여 권한 승인 |

---

## 12. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| `setupAll` 실행 시 "스프레드시트에 바인딩된 상태에서만 동작" | standalone Apps Script로 열림 | 스프레드시트를 먼저 열고 메뉴에서 Apps Script 진입 |
| `setupAll` 실행 시 `예약내역 시트에 기존 데이터가 있는데 헤더가 다릅니다` | 구 양식(36컬럼)이 남아있음 | 백업 후 [예]로 진행하거나 시트를 비우고 재실행 |
| 가격이 모두 0원 | 설정 시트 누락 또는 `migrateSettings_260522` 미실행 | `setupAll_keepData()` 실행하여 누락분 보강 |
| 평일·주말 가격이 같음 | 폼에서 날짜 미선택 | 정상 동작 (날짜 선택 시 즉시 갱신됨) |
| 이메일 입력칸이 없음 | index.html 구 버전 | index.html 최신 내용으로 재배포 |
| 함수 드롭다운에 `suggestRoomType` 등 옛 함수가 보임 | 구 `.gs` 파일이 남아있음 | 좌측 파일 목록에서 10개 외 파일 삭제 |
| Calendar 이벤트 생성/동기화 문제 | — | 11절 캘린더 연동 절차 / 11-6 트러블슈팅 참고 |
| 약관 모달이 명세와 다름 | `getTermsContent`가 외부 Docs 의존 (구버전) | 현재 약관은 `70_Integration.gs::TERMS_HTML_260522` 상수에 코드로 박혀 있음. 신규 코드 재배포 |
| 환불 계산 항상 0% | `reservationDate` 포맷 오류 | YYYY-MM-DD 문자열로 전달 (`formatDate()` 사용) |
| `_verifyPhase0` 결과의 `cols.length=25` 실패 | 컬럼이 25 아님 | `setupAll`을 다시 실행하거나 시트 헤더 직접 확인 |

---

## 13. 롤백

### 코드 롤백
1. Apps Script 에디터에서 새 10개 파일 모두 삭제 → 백업한 구 `Code.gs` 또는 9개 파일 재반영 → 새 버전 배포.

### 시트 롤백
2-1에서 만든 사본을 운영용으로 승격합니다. 그 사이 신규 예약이 있었다면 수동 이관.

---

## 부록 A: 약관 ↔ 코드 매핑

| 약관/확정안 항목 | 구현 위치 |
|---|---|
| 기준 인원 4명 / 최대 8명 | `10_Config.gs::migrateSettings_260522` + `30_Reservation.gs::validateFormData` + `index.html` |
| 최소 3시간 / 24시간 운영 | `40_Pricing.gs::calculateTotalPrice` + `30_Reservation.gs::validateFormData` + `index.html::generateTimeOptions` |
| 평일·주말 차등 요금 | `40_Pricing.gs::isWeekendOrHoliday + calculateTotalPrice` |
| 시간 추가 50,000원 / 인원 추가 10,000원 | `40_Pricing.gs::calculateTotalPrice` |
| 보증금 100,000원 | `40_Pricing.gs` + `30_Reservation.gs` + `index.html` 가격 행 |
| VAT 포함 가격 | `40_Pricing.gs` (역산) + UI 가격 행 |
| 환불 규정 (8/5-7/3-4/2일이내) | `40_Pricing.gs::calculateRefundAmount` + 설정 시트 4개 키 |
| 약관 본문 | `70_Integration.gs::TERMS_HTML_260522` (코드 상수) |
| eoacro 캘린더 등록 | `70_Integration.gs::createCalendarEvent` + `onEdit` 자동 트리거 + `onOpen` 메뉴 + `CALENDAR_ID_CONFIRMED` |
| 세금계산서 운영 | 폼 세금계산서 항목 + 사업자등록증 업로드 + `70_Integration.gs::uploadFile` |

## 부록 B: clasp 사용자

```bash
clasp pull   # 기존 환경 백업
# 본 저장소의 .gs + index.html을 .clasp 디렉토리로 복사 (구 파일은 삭제)
clasp push   # 일괄 반영
```
이후 본 가이드 4절(`setupAll`)로 진행합니다.

## 부록 C: 실행 순서 체크리스트

- [ ] 2. 스프레드시트 사본 + 기존 코드 백업
- [ ] 3. 10개 `.gs` 파일 + `index.html` Apps Script에 반영
- [ ] 4. `setupAll()` 실행 → 경고창에서 [예]
- [ ] 5. (선택) 공휴일 등록
- [ ] 6. `_verifyPhase0()` 실행 → 모든 항목 통과
- [ ] 7. Properties (ADMIN_EMAIL / ADMIN_PHONE / CALENDAR_ID_CONFIRMED) 입력
- [ ] 8. `testCalendarPermission` 실행으로 캘린더 권한 승인
- [ ] 9. WebApp 재배포
- [ ] 10. 수동 회귀 시나리오 11종 검증
